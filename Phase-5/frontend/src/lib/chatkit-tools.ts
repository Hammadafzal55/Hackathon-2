/**
 * This file is kept as a placeholder.
 * Tool calls are handled server-side by the FastAPI backend's AI agent.
 * The backend processes tool calls (create_task, list_tasks, etc.) and returns
 * results in the ChatResponse.tool_calls array.
 *
 * If ChatKit hosted mode is used in the future (with a valid domain key),
 * this file can be restored to handle onClientTool callbacks.
 */

export {};
